#pragma once

/**
 * Runs the photo filesystem. This function blocks until otherwise interrupted.
 * @param location The location where the filesystem should be mounted
 */
void filesystem_run(char* location);
